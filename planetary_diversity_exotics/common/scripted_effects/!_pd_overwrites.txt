# LIOS placeholder file. Filename starts with ! so it sorts first and any later file in the same common/scripted_effects/ folder (across mods) overrides these stubs. These effect names are referenced by decision overwrites in this submod (planetary_diversity_exotics) inside the non-cryo branch that replicates the original vanilla / PD decision content verbatim; when the planetary_diversity_arcologies submod is loaded its real definitions in pd_arcology_effects.txt win, when it is not loaded these empty no-op stubs keep the references resolvable.

generate_pd_arcology_portrait_effect = {
}

generate_pd_hive_arc_portrait_effect = {
}

generate_pd_machine_arc_portrait_effect = {
}
