# Real implementations of the cross-mod triggers stubbed in PD Arcologies' !_pd_exotic_compat.txt. Loaded only when this submod is present; LIOS resolves the always-no stubs to these definitions because the filename sorts after !_pd_exotic_compat.txt.

pd_is_ammonia_planet      = { is_planet_class = pc_pd_ammonia }
pd_has_ammonia_atmosphere = { has_modifier = pd_ammonia_atmosphere }
pd_is_methane_planet      = { is_planet_class = pc_pd_methane }
pd_has_methane_atmosphere = { has_modifier = pd_methane_atmosphere }
