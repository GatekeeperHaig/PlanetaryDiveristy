# Stub modifiers so PD main's planet-class conditional_icon blocks can reference pd_floating/pd_crystal/pd_biosynth without log errors when the Unique Worlds submod isn't loaded.
# Unique Worlds' real definitions in pd_unique_static_modifiers.txt override these (later mod wins), and only that submod ever applies them, so these stubs never reach a planet.

pd_floating = {}
pd_crystal = {}
pd_biosynth = {}
