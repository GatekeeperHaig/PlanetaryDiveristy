# Bio-synthetic species type for the origin_pd_biosynth start. Climate-neutral on purpose: no removed/added planet types, so bio-synths keep normal biological habitability and start on the alpine pc_pd_biosynth.
BIOSYNTH = {
	archetype = BIOLOGICAL
	# Blocked to Machine Age: Unit 04 and the bio-synth portraits are Machine Age content.
	playable = { has_machine_age_dlc = yes }
	# Standalone-test mode: origin gate temporarily disabled so BIOSYNTH is freely selectable. Re-enable `origin = { value = origin_pd_biosynth }` once Phase 2 adds the origin.
	possible = {
		authority = { NOT = { value = auth_machine_intelligence } }
		# origin = { value = origin_pd_biosynth }
	}
	randomized = { always = no }
	gender = yes
	graphical_culture = molluscoid_01
	generate_shipset = no
	move_pop_sound_effect = "molluscoid_pops_move"
	resources = {}
	trait = "trait_biosynth"
}
