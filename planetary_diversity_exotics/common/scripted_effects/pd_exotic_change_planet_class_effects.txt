# Mirrors PD main's pd_*_world_effect helpers for the exotic cryogenic classes. These are real planet classes, so the class sets its own entity; the helper only sets the class and its label modifier.

pd_ammonia_world_effect = {
	change_pc = pc_pd_ammonia
	add_modifier = {
		modifier = pd_ammonia
		days = -1
	}
}

pd_methane_world_effect = {
	change_pc = pc_pd_methane
	add_modifier = {
		modifier = pd_methane
		days = -1
	}
}

pd_nitrogen_world_effect = {
	change_pc = pc_pd_nitrogen
	add_modifier = {
		modifier = pd_nitrogen
		days = -1
	}
}
