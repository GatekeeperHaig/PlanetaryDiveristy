# Chemovat Garden district flavor description. First match wins; mirrors the name script structure.

triggered_flavor_desc = {
	trigger = {
		num_zones = {
			type = zone_pdexotic_cryo_chemovat_hydride_synthesis
			value > 0
		}
	}
	text = zone_pdexotic_cryo_chemovat_hydride_synthesis_desc
}

triggered_flavor_desc = {
	trigger = {
		num_zones = {
			type = zone_pdexotic_cryo_chemovat_refinement
			value > 0
		}
	}
	text = zone_pdexotic_cryo_chemovat_refinement_desc
}

# Unconditional fallback when no chemovat specialization zone is placed yet.
triggered_flavor_desc = {
	text = pdexotic_cryo_chemovat_basic_desc
}
