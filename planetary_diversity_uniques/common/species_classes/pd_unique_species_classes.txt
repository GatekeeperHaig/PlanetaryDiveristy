# Biosynthetic species type for the origin_pd_biosynth start. Climate-neutral on purpose: no removed or added planet types, so it keeps normal biological habitability and starts on the alpine pc_pd_biosynth.
BIOSYNTH = {
	archetype = BIOLOGICAL
	# Blocked to Machine Age: Unit 04 and the Biosynthetic portraits are Machine Age content.
	playable = { has_machine_age_dlc = yes }
	# Only selectable alongside origin_pd_biosynth, since the species is infertile and has no pop source outside that origin's assembler.
	possible = {
		authority = { NOT = { value = auth_machine_intelligence } }
		origin = { value = origin_pd_biosynth }
	}
	randomized = { always = no }
	gender = yes
	graphical_culture = molluscoid_01
	generate_shipset = no
	move_pop_sound_effect = "molluscoid_pops_move"
	resources = {}
	trait = "trait_biosynth"
}
