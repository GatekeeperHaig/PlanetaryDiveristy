# Shared boilerplate for a rare-world custom WORKER job (regular empires). Category/swappable_data/tags/resources are set per job.
possible_pre_triggers = {
	has_owner = yes
	is_being_purged = no
	is_being_assimilated = no
	is_sapient = yes
}
possible_precalc = can_fill_worker_job
possible = {
	hidden_trigger = { exists = owner }
	owner = { is_gestalt = no }
}
weight = {
	weight = @worker_job_weight
}
