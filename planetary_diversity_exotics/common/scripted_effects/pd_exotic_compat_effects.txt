# Real implementations of the cross-mod scripted effects stubbed in PD Arcologies' !_pd_exotic_compat.txt. LIOS resolves the empty stubs to these definitions because this filename sorts after the arcologies stub.

pd_add_ammonia_atmosphere_effect = {
	add_modifier = {
		modifier = pd_ammonia_atmosphere
		days = -1
	}
}

pd_add_methane_atmosphere_effect = {
	add_modifier = {
		modifier = pd_methane_atmosphere
		days = -1
	}
}
