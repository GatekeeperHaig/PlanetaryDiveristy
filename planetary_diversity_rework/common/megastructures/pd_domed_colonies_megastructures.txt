pd_domed_colony_0 = {
	entity = ""
	construction_entity = ""
	portrait = "GFX_megastructure_construction_background"
	place_entity_on_planet_plane = yes
	show_galactic_map_icon = no
	build_time = 1800
	resources = {
		category = megastructures
		cost = {
			alloys = 1500
		}
	}
	construction_blocks_and_blocked_by = none

	potential = {
		has_technology = tech_pd_domed_colonies
	}

	possible = {
		exists = starbase
		custom_tooltip = {
			fail_text = "requires_inside_border"
			is_inside_border = from
		}
	}

	placement_rules = {
		planet_possible = {
			custom_tooltip = {
				fail_text = "requires_surveyed_planet"
				is_surveyed = {			# prevent leaking habitability information
					who = prev.from
					status = yes
				}
			}
			custom_tooltip = {
				fail_text = "requires_no_anomaly"
				NOT = { has_anomaly = yes }
			}
			custom_tooltip = {
				fail_text = "requires_no_existing_megastructure"
				#can_build_megastructure_on_planet = yes
				NOR = {
					has_planet_flag = megastructure
					#has_planet_flag = has_megastructure
					solar_system = {
						has_star_flag = ring_world_built
					}
					is_planet_class = pc_ringworld_habitable
					is_planet_class = pc_ringworld_habitable_damaged
					is_planet_class = pc_ringworld_tech
					is_planet_class = pc_ringworld_tech_damaged
					is_planet_class = pc_ringworld_seam
					is_planet_class = pc_ringworld_seam_damaged
					is_planet_class = pc_habitat
				}
			}
			custom_tooltip = {
				fail_text = "requires_terraforming_candidate"
				OR = {
					has_modifier = terraforming_candidate
					has_modifier = frozen_terraforming_candidate
					has_modifier = toxic_terraforming_candidate
				}
			}
			custom_tooltip = {
				fail_text = "requires_correct_planet_class"
				OR = {
					is_planet_class = pc_barren
					is_planet_class = pc_barren_cold
					is_planet_class = pc_toxic
					is_planet_class = pc_frozen
					is_planet_class = pc_pd_barren_superhabitable
					is_planet_class = pc_pd_barren_tidally_locked
					is_planet_class = pc_pd_barren_cave
					is_planet_class = pc_hothouse
				}
			}
		}
	}

	# root = system
	# from = country
	ai_weight = {
		factor = 0
	}
	#inline_script = "megastructures/pd_where_ai_can_build_phabs"

	on_build_start = {}
	on_build_cancel = {}

	on_build_complete = {
		fromfrom.planet = {
			if = {
				limit = { has_orbital_station = yes }
				orbital_station = {
					dismantle = yes
				}
			}
			set_planet_flag = colony_event
			clear_blockers = yes

			#Barren
			if = {
				limit = {
					is_planet_class = pc_barren
				}
				change_pc = pc_pd_domed_colony_barren
				if = {
					limit = {
						is_moon = yes
					}
					set_planet_entity = {
						picture = pc_pd_domed_colony_barren_moon
					}
				}
				if = {
					limit = {
						has_planet_flag = pdmars
					}
					set_planet_entity = {
						picture = pc_pd_domed_colony_mars
						entity = domed_colony_barren_planet_mars_entity
					}
				}
			}
			#Barren Cold
			if = {
				limit = {
					is_planet_class = pc_barren_cold
				}
				change_pc = pc_pd_domed_colony_barren_cold
			}
			#Frozen
			if = {
				limit = {
					is_planet_class = pc_frozen
				}
				change_pc = pc_pd_domed_colony_frozen
			}
			#Toxic
			if = {
				limit = {
					OR = {
						is_planet_class = pc_toxic
						is_planet_class = pc_hothouse
					}
				}
				change_pc = pc_pd_domed_colony_toxic
				if = {
					limit = {
						has_planet_flag = pdvenus
					}
					set_planet_entity = {
						entity = domed_colony_toxic_planet_venus_entity
					}
				}
			}
			#Superhabitable
			if = {
				limit = {
					is_planet_class = pc_pd_barren_superhabitable
				}
				change_pc = pc_pd_domed_colony_superhabitable
			}
			#Tidally Locked
			if = {
				limit = {
					is_planet_class = pc_pd_barren_tidally_locked
				}
				change_pc = pc_pd_domed_colony_tidally_locked
			}
			#Cave
			if = {
				limit = {
					is_planet_class = pc_pd_barren_cave
				}
				change_pc = pc_pd_domed_colony_cave
			}

			#Everything Else
			if = {
				limit = {
					planet_size > 25
				}
				add_modifier = {
					modifier = high_gravity days = -1
				}
			}
			if = {
				limit = {
					planet_size < 10
				}
				add_modifier = {
					modifier = low_gravity days = -1
				}
			}
			if = {
				limit = {
					OR = {
						has_modifier = terraforming_candidate
						has_modifier = frozen_terraforming_candidate
						has_modifier = toxic_terraforming_candidate
					}
				}
				remove_modifier = terraforming_candidate
				remove_modifier = frozen_terraforming_candidate
				remove_modifier = toxic_terraforming_candidate
				add_modifier = { modifier = pd_domed_colony days = -1 }
				add_modifier = { modifier = hab_terraforming_candidate days = -1 }
				reroll_deposits = yes
				clear_blockers = yes
				remove_deposit = d_prospectorium_strip_mine
			}
		}
		remove_megastructure = fromfrom
		# TODO ADD Notification or Toast
		#from = { country_event = { id = megastructures.10 } }
		#from = { country_event =  { id = moonbase.00 } }
	}
}

pd_domed_base_0 = {
	entity = "invisible_turret_entity"
	construction_entity = "invisible_turret_entity"
	# entity = "domed_colony_barren_cold_planet"
	# construction_entity = "domed_colony_barren_cold_planet"
	portrait = "GFX_megastructure_construction_background"
	place_entity_on_planet_plane = yes
	entity_offset = { x = 0 y = 0 }
	rotate_to_center = no
	scale_offset = yes
	#show_in_outliner = no
	#use_planet_resource = yes
	scales_with_planet = yes
	build_time = 900
	resources = {
		category = megastructures
		cost = {
			alloys = 750
		}
	}

	potential = {
		has_technology = tech_pd_domed_colonies
	}

	potential = {
		always = yes
	}

	possible = {
		exists = starbase
		custom_tooltip = {
			fail_text = "requires_inside_border"
			is_inside_border = from
		}
		custom_tooltip = {
			fail_text = "requires_surveyed_system"
			NOT = {
				any_system_planet = {
					is_surveyed = {
						who = prev.from
						status = no
					}
				}
			}
		}
		custom_tooltip = {
			fail_text = "requires_no_existing_megastructure"
			has_no_non_gate_megastructure = yes
		}
		custom_tooltip = {
			fail_text = "has_terraforming_candidate"
			NOR = {
				has_modifier = terraforming_candidate
				has_modifier = frozen_terraforming_candidate
				has_modifier = toxic_terraforming_candidate
			}
		}
	}

	placement_rules = {
		planet_possible = {
			custom_tooltip = {
				fail_text = "requires_no_anomaly"
				NOT = { has_anomaly = yes }
			}
			custom_tooltip = {
				fail_text = "pd_domed_habitat_rules"
				pd_has_domed_habitat_modifier = yes
			}
			custom_tooltip = {
				fail_text = "requires_no_existing_megastructure"
				NOT = { has_planet_flag = megastructure }
			}
		}
	}

	# root = system
	# from = country
	ai_weight = {
		factor = 0

		modifier = {
			factor = 0.1
			starbase = { NOT = { has_starbase_size >= starbase_starfortress } }
		}

		modifier = {
			factor = 0.1
			any_neighbor_system = {
				exists = owner
				NOT = {
					owner = { is_same_value = from }
				}
			}
		}
	}

	on_build_start = {}
	on_build_cancel = {}
	on_build_complete = {
		fromfrom.planet = {
			if = {
				limit = { has_orbital_station = yes }
				orbital_station = {
					dismantle = yes
				}
			}
			set_planet_flag = colony_event
			clear_blockers = yes
			remove_megastructure = fromfrom
			if = {
				limit = {
					has_orbital_energy_deposit = yes
				}
				set_planet_flag = d_pd_habitat_energy
			}
			if = {
				limit = {
					has_orbital_mining_deposit = yes
				}
				set_planet_flag = d_pd_habitat_mining
			}
			if = {
				limit = {
					has_deposit_for = shipclass_research_station
				}
				set_planet_flag = d_pd_habitat_research
			}
			clear_deposits = yes
			if = {
				limit = {
					has_planet_flag = d_pd_habitat_energy
				}
				remove_planet_flag = d_pd_habitat_energy
				add_deposit = d_pd_habitat_energy
			}
			if = {
				limit = {
					has_planet_flag = d_pd_habitat_mining
				}
				remove_planet_flag = d_pd_habitat_mining
				add_deposit = d_pd_habitat_mining
			}
			if = {
				limit = {
					has_planet_flag = d_pd_habitat_research
				}
				remove_planet_flag = d_pd_habitat_research
				add_deposit = d_pd_habitat_research
			}
			# #Barren
			# if = {
			# 	limit = {
			# 		is_planet_class = pc_barren
			# 	}
			# 	change_pc = pc_pd_domed_colony_barren
			# 	if = {
			# 		limit = {
			# 			is_moon = yes
			# 		}
			# 		set_planet_entity = {
			# 			picture = pc_pd_domed_colony_barren_moon
			# 		}
			# 	}
			# }
			#Barren Cold
			if = {
				limit = {
					is_planet_class = pc_barren_cold
				}
				change_pc = pc_pd_domed_habitat_barren_cold
				if = {
					limit = {
						has_planet_flag = pdluna
					}
					set_planet_entity = {
						entity = domed_colony_barren_cold_planet_luna_entity
					}
				}
			}
			# #Frozen
			# if = {
			# 	limit = {
			# 		is_planet_class = pc_frozen
			# 	}
			# 	change_pc = pc_pd_domed_colony_frozen
			# }
			# #Toxic
			# if = {
			# 	limit = {
			# 		OR = {
			# 			is_planet_class = pc_toxic
			# 			is_planet_class = pc_hothouse
			# 		}
			# 	}
			# 	change_pc = pc_pd_domed_colony_toxic
			# 	if = {
			# 		limit = {
			# 			has_planet_flag = pdvenus
			# 		}
			# 		set_planet_entity = {
			# 			entity = domed_colony_toxic_planet_venus_entity
			# 		}
			# 	}
			# }
			#Molten
			#Gas Giant
			add_modifier = { modifier = pd_domed_habitat days = -1 }
			pd_remove_domed_habitat_modifier = yes
		}
		remove_megastructure = fromfrom
	}
}