# Placeholder effects so this submod's non-cryo decision branches resolve when PD Arcologies isn't loaded; this ! file sorts before Arcologies' real definitions, which override it when present.

generate_pd_arcology_portrait_effect = {
}

generate_pd_hive_arc_portrait_effect = {
}

generate_pd_machine_arc_portrait_effect = {
}
