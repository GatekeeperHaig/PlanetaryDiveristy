# Chemovat Garden district subtitle. First match wins; slot_pdexotic_cryo_chemovat only accepts the two zones below.

triggered_name = {
	trigger = {
		num_zones = {
			type = zone_pdexotic_cryo_chemovat_hydride_synthesis
			value > 0
		}
	}
	text = zone_pdexotic_cryo_chemovat_hydride_synthesis
}

triggered_name = {
	trigger = {
		num_zones = {
			type = zone_pdexotic_cryo_chemovat_refinement
			value > 0
		}
	}
	text = zone_pdexotic_cryo_chemovat_refinement
}

# Unconditional fallback when no chemovat specialization zone is placed yet.
triggered_name = {
	text = pdexotic_cryo_chemovat_basic
}
